<%= form_for(@course_timetable) do |f| %>
  <% if @course_timetable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_timetable.errors.count, "error") %> prohibited this course_timetable from being saved:</h2>

      <ul>
      <% @course_timetable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
