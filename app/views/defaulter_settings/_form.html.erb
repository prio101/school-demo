<%= form_for(@defaulter_setting) do |f| %>
  <% if @defaulter_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@defaulter_setting.errors.count, "error") %> prohibited this defaulter_setting from being saved:</h2>

      <ul>
      <% @defaulter_setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :current %><br>
    <%= f.check_box :current %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
