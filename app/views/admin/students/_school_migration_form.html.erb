<%= form_tag(school_migration_admin_student_path(student), method: :put) do %>
    <%= select_tag :school_id, options_from_collection_for_select(School.all, "id", "name", student.school) %>
    <%= submit_tag 'Migrate' %>
<% end %>